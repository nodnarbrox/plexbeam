# PlexBeam GPU Worker Configuration
# Copy to .env and adjust for your setup

# Hardware acceleration: qsv | nvenc | vaapi | none
PLEX_WORKER_HW_ACCEL=qsv

# Server
PLEX_WORKER_HOST=0.0.0.0
PLEX_WORKER_PORT=8765

# Intel QSV settings
# PLEX_WORKER_QSV_DEVICE=/dev/dri/renderD128
# PLEX_WORKER_QSV_PRESET=veryfast
# PLEX_WORKER_QSV_QUALITY=25
# PLEX_WORKER_QSV_LOW_POWER=true          # Use VDBOX fixed-function encode (faster)

# NVIDIA NVENC settings
# PLEX_WORKER_NVENC_PRESET=p1             # p1=fastest, p7=highest quality
# PLEX_WORKER_NVENC_TUNE=ull              # hq | ll (low latency) | ull (ultra-low latency)
# PLEX_WORKER_NVENC_GPU=0

# Concurrency (Tesla cards have no NVENC session limit — can go higher)
# PLEX_WORKER_MAX_CONCURRENT_JOBS=2

# Media path mapping (container paths -> host paths)
# PLEX_WORKER_MEDIA_PATH_FROM=/media
# PLEX_WORKER_MEDIA_PATH_TO=/mnt/media
# PLEX_WORKER_PATH_MAPPINGS=/config=/mnt/config;/tmp=/mnt/tmp

# Plex server URL (for fetching media)
# PLEX_WORKER_PLEX_SERVER_URL=http://192.168.1.100:32400

# Logging
# PLEX_WORKER_LOG_LEVEL=INFO
# PLEX_WORKER_LOG_FFMPEG_OUTPUT=true
